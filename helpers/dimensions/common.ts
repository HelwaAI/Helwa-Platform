/**
 * Represents a position and length in bitmap coordinates.
 */
export interface BitmapPositionLength {
  /** The position (x or y coordinate) in bitmap pixels */
  position: number;
  /** The length (width or height) in bitmap pixels */
  length: number;
}
